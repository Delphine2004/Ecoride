<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirige toutes les requêtes vers api.php si le fichier ou dossier n'existe pas
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^ api.php [QSA,L]

</IfModule>
